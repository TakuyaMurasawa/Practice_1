puts "にんゃわんこ#{name}"

if number == 0
    puts "hit"
elsif number == 1
    puts "1hit"
else
    puts "nohit"
end

    puts "こんにちは#{name}"


puts "何か入力してください"
 a = gets
puts "入力された内容は#{a}"
# 実行
#1.キーボードから何か入力してみましょう
#2.キーボードから何か入力してみましょう
#   hello
#   入力された内容はhello

while 条件 do
    処理  # 条件がtrueの間処理を繰り返す
  end
  
dice = 0  # 変数diceに0を代入し、初期値を設定する

while dice != 6 do  # サイコロの目が6ではない間、diceの初期値は0なので条件を満たす。以降はdiceに代入される数によって結果が異なる
  dice = rand(1..6)  # 1～6の数字がランダムに出力される
  puts dice
end